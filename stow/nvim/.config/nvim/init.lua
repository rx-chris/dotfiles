require("core.options") -- load options configuration
require("core.clipboard") -- load clipboard options (environment-specificc)
require("core.keymaps") -- load keymaps configuration
require("core.lazy") -- load lazy.nvim
